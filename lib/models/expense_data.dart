import 'package:flutter/material.dart';

class ExpenseData{
final String name;
final double amount;
final int percentage;
final Color color;
final Icon icon;

 const ExpenseData({this.name, this.amount, this.percentage, this.color,this.icon});
}